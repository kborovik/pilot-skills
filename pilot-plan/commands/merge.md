---
description: Merge a GitHub pull request into main with a release-note-ready squash commit. Defaults to current branch's PR.
argument-hint: [PR number]
---

Run the procedure in `<plugin-dir>/skills/github-pr-merge/SKILL.md` with `$ARGUMENTS`.
