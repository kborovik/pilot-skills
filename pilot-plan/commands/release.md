---
description: Cut a SemVer release for a single-package repo. Auto-detects version manifest, bumps, commits, creates annotated tag `v<x.y.z>`. Local only — does not push.
argument-hint: [patch|minor|major|x.y.z]
---

Run the procedure in `<plugin-dir>/skills/github-release/SKILL.md` with `$ARGUMENTS`.
