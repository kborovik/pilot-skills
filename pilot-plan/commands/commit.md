---
description: Commit currently staged git changes with a Conventional Commits message that matches repo style.
argument-hint: [commit message hint]
---

Run the procedure in `<plugin-dir>/skills/github-commit-staged/SKILL.md` with `$ARGUMENTS`.
