---
description: Drift detector. Diff SPEC.md against code. Read-only, zero writes.
argument-hint: [§V | §I | §T | --all]
---

Run the procedure in `<plugin-dir>/skills/check/SKILL.md` with `$ARGUMENTS`.
