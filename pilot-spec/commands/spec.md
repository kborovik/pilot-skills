---
description: Create, amend, or backprop bug into SPEC.md. Sole mutator of spec — always-gated by socratic skill, mode emitted by gate post-convergence.
argument-hint: [<free-form intent>]
---

Run the procedure in `<plugin-dir>/skills/spec/SKILL.md` with `$ARGUMENTS`.
