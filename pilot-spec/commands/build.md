---
description: Plan-then-execute against SPEC.md. Native Claude Code loop, no sub-agents.
argument-hint: [§T.n | --all | --next | --fast | --plan]
---

Run the procedure in `<plugin-dir>/skills/build/SKILL.md` with `$ARGUMENTS`.
